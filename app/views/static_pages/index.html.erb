<% provide(:title, 'All users') %>
<h1>All users and the amount of points they have based on their contributions</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= user.name %>
      <%= user.karma %>
    </li> 
  <% end %>
</ul>
